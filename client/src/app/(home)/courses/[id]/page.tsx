"use client";

import SingleCourseSection from "@/src/components/features/course/SingleCourseSection";

const page = () => {
  //   const { id } = useParams();
  //   const { data, isLoading, isFetching } = useGetSingleCourseAdminQuery(id as string);
  return (
    <div>
      <SingleCourseSection />
    </div>
  );
};

export default page;
